const array2D = [
    [1, 2, 3],
    [4, 5, 6],
    [7, 8, -9]
];


function sum2DArray(arr) {
    return new Promise((resolve, reject) => {
        return new Promise((resolve, reject) =>
        {
            if (arr.length > rowIdx) {
                setTimeout(() => {
                    arr[rowidx].filter((e) => {
                        return e < 0;
                    })
                }, 0);
            }
        })
    });
}

negsPerRowPromies = [];

for (let x = 0; )

Promise.any(negsPerRowPromies)
    .then((results) => {
        console.log(results);
    })
    .catch((error) => console.log('Error Msg: $(error)'));